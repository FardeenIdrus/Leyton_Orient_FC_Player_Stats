"""Streamlit dashboard (Phase 8).

Position selector, ranked shortlist, single-player profile (percentile bars),
side-by-side comparison, and adjustable constraint controls. Wired to Postgres.
"""

# Phase 8: intentionally empty until the model layers exist.

