# Assets

Drop the official Leyton Orient crest here as **`logo.png`** (a square PNG with a
transparent background works best). The dashboard shows it in the header automatically;
until it is present, a styled text wordmark is shown instead.

The crest is club property — add it for internal/demo use; it is not bundled here.
